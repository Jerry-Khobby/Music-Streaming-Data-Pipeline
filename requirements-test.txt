# Test runner and coverage
pytest==8.1.1
pytest-cov==5.0.0

# AWS service mocking — intercepts boto3 calls without hitting real AWS
moto[s3,dynamodb,sqs,sns]==5.0.2

# AWS SDK used by archive_job and dynamodb_loader
boto3==1.34.69

# PySpark must be installed for the system Python (/usr/bin/python3).
# The apache/spark image ships PySpark under Spark's own runtime, not the
# system pip, so pytest cannot find it without this explicit install.
pyspark==3.5.0

# Used by monitoring/notifier.py to send Slack failure alerts
requests==2.31.0

# Load .env credentials when running scripts locally
python-dotenv==1.0.1
